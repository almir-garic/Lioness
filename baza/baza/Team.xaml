<Page
    x:Class="AppCatalog.TeamPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppCatalog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Save" Label="Sačuvaj" Name="SaveAppBarButton" Click="SaveAppBarButton_Click" />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox x:Name="NameTextBox" Margin="10" TextWrapping="Wrap" Text="" VerticalAlignment="Top" PlaceholderText="Name"/>
        <TextBox x:Name="DescriptionTextBox" Margin="10,47,10,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" PlaceholderText="Description"/>

    </Grid>
</Page>
